<UserControl
    x:Class="UnoTest.Client.Controls.DateControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:iRatio.Client.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock"/> <!-- ComboBox Arrow Uno Fix-->
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <ComboBox x:Name="YearBox" HorizontalContentAlignment="Center"/>
        <ComboBox x:Name="MonthBox" Grid.Column="1" HorizontalContentAlignment="Center"/>
        <ComboBox x:Name="DayBox" Grid.Column="2" HorizontalContentAlignment="Center"/>
    </Grid>
</UserControl>
